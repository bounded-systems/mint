# only exceptions are  push) echo "triggerand repository_dispatch). Consumers'
             push) echo "trigger| push) echo "trigger '$EVENT' is trusted" ;;
                   "Call it from  push) echo "trigger(see this workflow's header)."
